{
  "name": "zadarma-call-router",
  "version": "1.0.0",
  "description": "Автообработка входящих звонков: первый звонок — автоответ, повторный через N минут — соединение с владельцем, повтор внутри окна — игнорирование.",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test tests/*.test.js",
    "setup:zadarma": "node scripts/zadarma-setup.mjs",
    "simulate": "node scripts/simulate-call.mjs"
  },
  "keywords": [
    "zadarma",
    "webhook",
    "ivr",
    "call-routing",
    "telephony"
  ],
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {}
}
