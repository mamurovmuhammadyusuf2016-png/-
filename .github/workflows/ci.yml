name: CI

on:
  push:
    branches: ['**']
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Проверка синтаксиса
        run: |
          for file in $(find src scripts -name '*.js' -o -name '*.mjs'); do
            node --check "$file"
          done

      - name: Тесты
        run: npm test
