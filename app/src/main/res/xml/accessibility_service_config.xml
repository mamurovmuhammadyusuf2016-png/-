<?xml version="1.0" encoding="utf-8"?>
<!--
  flagIncludeNotImportantViews is deliberately absent: it floods the tree with decorative
  views, which pushes the elements that matter past the traversal cap and out of the
  description the planner sees.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged|typeViewFocused|typeViewScrolled"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagReportViewIds"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_description"
    android:notificationTimeout="200" />
